// backend/validators/attributeCatalog.js

// Define the allowed attributes for rule creation
const validAttributes = ['age', 'salary', 'experience', 'department', 'position'];

module.exports = { validAttributes };
